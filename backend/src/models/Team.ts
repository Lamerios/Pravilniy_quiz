/**
 * Team model interface
 * Represents a team in the quiz game
 */

import { Team, CreateTeamRequest, UpdateTeamRequest } from '../types/shared';

export { Team, CreateTeamRequest, UpdateTeamRequest };
