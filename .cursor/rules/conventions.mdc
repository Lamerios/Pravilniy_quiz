# 📋 conventions.md - Правила разработки кода

> **Технические соглашения для code ассистента**
> 
> Основаны на принципах из [@vision.md](./vision.md) - обязательно изучи этот документ перед началом работы

---

## 🎯 Главные принципы

### **KISS - Keep It Simple, Stupid**
- Выбирай простейшее решение, которое работает
- При сомнениях - более простой путь
- Никаких "а что если в будущем..."

### **Одна функция за раз**
- Реализуй полностью одну фичу от API до UI
- НЕ начинай новую, пока предыдущая не работает
- Тестируй перед переходом к следующей

### **Вертикальные срезы**
- База данных → API → UI для одной функции
- НЕ все API сразу, потом весь фронтенд

---

## 📁 Файловые ограничения

### **Размер файлов:**
- ❌ Файлы больше 200 строк - разбивай на модули
- ❌ Более 3-4 новых файлов за раз
- ✅ Один компонент = один файл

### **Структура:**
- Максимум 3 уровня вложенности
- Имена папок в kebab-case
- Имена файлов описывают содержимое

---

## 🔧 Правила кода

### **TypeScript:**
```typescript
// ✅ Правильно - явные типы
interface CreateGameRequest {
  name: string;
  templateId: number;
  teamIds: number[];
}

// ❌ Неправильно - any types
const createGame = (data: any) => { ... }
```

### **React компоненты:**
```typescript
// ✅ Правильно - функциональные компоненты + хуки
export const GameBoard: React.FC<GameBoardProps> = ({ gameId }) => {
  const [scores, setScores] = useState<Score[]>([]);
  // ...
};

// ❌ Неправильно - классовые компоненты
class GameBoard extends React.Component { ... }
```

### **API endpoints:**
```typescript
// ✅ Правильно - RESTful + валидация
router.post('/games', validateCreateGame, async (req, res) => {
  try {
    const game = await gameService.create(req.body);
    res.status(201).json(game);
  } catch (error) {
    logger.error('Game creation failed', { error: error.message });
    res.status(500).json({ error: 'Internal server error' });
  }
});
```

---

## 📝 Документация кода

### **JSDoc для всех функций:**
```typescript
/**
 * Создает новую игру на основе шаблона
 * @param gameData - данные для создания игры
 * @param gameData.name - название игры
 * @param gameData.templateId - ID шаблона игры
 * @returns Promise с созданной игрой
 * @throws Error если шаблон не найден
 */
export async function createGame(gameData: CreateGameRequest): Promise<Game> {
  // ...
}
```

### **Комментарии для бизнес-логики:**
```typescript
// Расчет итогового счета: сумма баллов за все раунды
const totalScore = roundScores.reduce((sum, score) => sum + score.points, 0);

// Проверка, что игра активна перед обновлением счета
if (game.status !== 'active') {
  throw new Error('Cannot update scores for inactive game');
}
```

---

## 🧪 Тестирование каждого шага

### **Backend - каждый endpoint:**
```typescript
// tests/routes/games.test.ts
describe('POST /games', () => {
  it('should create game with valid data', async () => {
    const response = await request(app)
      .post('/games')
      .send(validGameData)
      .expect(201);
    
    expect(response.body.name).toBe(validGameData.name);
  });
});
```

### **Frontend - каждый компонент:**
```typescript
// components/__tests__/GameBoard.test.tsx
describe('GameBoard', () => {
  it('renders team scores correctly', () => {
    render(<GameBoard gameId={1} />);
    expect(screen.getByText('Team Alpha')).toBeInTheDocument();
  });
});
```

---

## 🔄 Рефакторинг сразу

### **Видишь дублирование - рефакторишь:**
```typescript
// ❌ Дублирование
const validateTeamName = (name: string) => name.length > 0 && name.length <= 100;
const validateGameName = (name: string) => name.length > 0 && name.length <= 100;

// ✅ Выносим в utils
export const validateName = (name: string, maxLength = 100) => 
  name.length > 0 && name.length <= maxLength;
```

### **Файл больше 200 строк - разбиваешь:**
```typescript
// ❌ Один большой файл
// gameService.ts (300+ lines)

// ✅ Разбиваем на модули
// services/game/gameService.ts
// services/game/gameValidation.ts  
// services/game/gameRepository.ts
```

---

## 🚦 Обработка ошибок

### **Backend:**
```typescript
// ✅ Правильно - логируем + возвращаем понятные ошибки
try {
  const game = await gameRepository.findById(gameId);
  if (!game) {
    logger.warn('Game not found', { gameId });
    return res.status(404).json({ error: 'Game not found' });
  }
} catch (error) {
  logger.error('Database error', { error: error.message, gameId });
  return res.status(500).json({ error: 'Internal server error' });
}
```

### **Frontend:**
```typescript
// ✅ Правильно - показываем пользователю понятные сообщения
const [error, setError] = useState<string>('');

try {
  await gameApi.updateScore(gameId, teamId, score);
} catch (err) {
  setError('Не удалось обновить счет. Попробуйте еще раз.');
  console.error('Score update failed', err);
}
```

---

## 📊 Соглашения по именованию

### **Переменные и функции:**
```typescript
// ✅ camelCase
const gameScore = 15;
const updateTeamScore = () => {};

// ✅ Глаголы для функций
const calculateTotalScore = () => {};
const fetchGameData = () => {};
```

### **Константы:**
```typescript
// ✅ UPPER_SNAKE_CASE
const MAX_TEAMS_PER_GAME = 20;
const DEFAULT_ROUND_SCORE = 0;
```

### **Типы и интерфейсы:**
```typescript
// ✅ PascalCase
interface GameData {
  id: number;
  name: string;
}

type ScoreUpdateRequest = {
  teamId: number;
  score: number;
};
```

---

## 🎬 Последовательность разработки

### **Порядок реализации (см. [@vision.md](./vision.md)):**
1. **Шаблоны игр** - CRUD операции
2. **Команды** - управление справочником
3. **Создание игр** - связывание команд и шаблонов
4. **Проведение игр** - обновление счета в реальном времени
5. **Публичное табло** - отображение результатов

### **Каждая фича = полный цикл:**
- База данных (миграции + модели)
- Backend API (endpoints + валидация + тесты)
- Frontend UI (компоненты + интеграция + тесты)

---

## ⚡ Запрещено

- ❌ Классовые React компоненты (только функциональные)
- ❌ Файлы больше 200 строк
- ❌ any типы в TypeScript
- ❌ Игнорирование ошибок (всегда обрабатывай)
- ❌ Комментарии на русском языке в коде (только JSDoc)
- ❌ Начинать новую фичу, пока предыдущая не работает

---

> 💡 **Главное правило**: Если сомневаешься - выбирай более простое решение и обращайся к [@vision.md](./vision.md) за контекстом.